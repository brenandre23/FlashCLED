# --- Python Standard ---
__pycache__/
*.py[cod]
*.class
*.so
.Python
env/
venv/
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# --- Jupyter ---
.ipynb_checkpoints

# --- Secrets (CRITICAL) ---
.env
.env.save
.env_windows
.venv
GoogleKeys/

# --- IDE & OS ---
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# --- Project Specific ---

# 1. Logs & Audit
logs/
data_audit/
data/raw/failed_windows_*.txt
data/raw/processing.log
/analysis/
deprecated/

# 2. Cache & Temp
cache/*
cached_originals/
!cache/.gitkeep

# All log files
*.log

# 3. Heavy/Generated Data
data/processed/*
data/raw/*
!data/processed/.gitkeep
!data/raw/.gitkeep

# 4. Temporary/Debug Scripts (Not for production)
clean_cache.py
troubleshoot_mines.py
RootList.py
check_como.py
rootlist.txt
WSL_command.txt
KDTree

# 5. Test outputs
test/*.png
test/*.csv

# 6. Models (too large for git, use git-lfs if needed)
models/*.pkl

# --- Local-Only (do not push to GitHub) ---
# Generated figure outputs (large PNGs/PDFs, regenerated from code)
Figures/
# LaTeX thesis (managed in separate Overleaf/git repo)
Overleaf/
# Model run archives (large parquet snapshots)
data/runs/
# Personal defense prep notes
Thesis Defense Lessons/
# Diagnostic text outputs
conflict_onset.txt
nlp_changes.txt
AI_ML_promptdiagnostics.txt
# Internal cleanup/audit docs (local reference only)
DEPRECATION_MANIFEST.md
# Local data quality work (not part of main pipeline)
ImputationDQ/
# Original analysis archive (pre-refactor, local reference only)
OG_Analysis/
# Generated plot directories (deleted; prevent recurrence)
agrimatrix_outputs/
# Gemini settings (deleted; prevent recurrence)
.gemini/
# Claude agent infrastructure (local only)
.claude/
